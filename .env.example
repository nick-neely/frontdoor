# Newsletter delivery. See docs/adr/0002-stateless-double-opt-in.md.
# All three are needed together: with any one missing the form says the
# newsletter is not configured yet rather than signing a link that dead-ends.
# RESEND_AUDIENCE_ID is the Resend audience the confirmed contact is created in
# and the audience `pnpm broadcast` drafts against.
RESEND_API_KEY=
RESEND_AUDIENCE_ID=

# Signs newsletter confirmation links. Generate with:
#   node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
# The signature is the only record that a subscription was requested - nothing
# is stored - and the timestamp inside it expires the link 24 hours after it was
# issued. Rotating this invalidates every confirmation link currently in flight.
NEWSLETTER_SIGNING_SECRET=

# Reads GitHub activity for the home page status line. Needs repository read
# access so private pushes are counted; private repository names are never
# rendered. Server-side only, never exposed to the client bundle.
GITHUB_ACTIVITY_TOKEN=

# Enables the hero parallax layer on the home page. Off unless set to "true".
# DESIGN.md permits parallax only as a bounded exception - a non-textual layer,
# at most 8% differential, transform-only - and says it is judged with eyes
# rather than in a specification. It ships only if it does not read cheap.
VITE_HERO_PARALLAX=
